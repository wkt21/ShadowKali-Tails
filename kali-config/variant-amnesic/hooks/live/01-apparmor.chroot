#!/bin/bash
set -euo pipefail

# Enable AppArmor
systemctl enable apparmor 2>/dev/null || true

# Best-effort enforce of available profiles
if command -v aa-enforce >/dev/null 2>&1; then
    aa-enforce /etc/apparmor.d/* 2>/dev/null || true
fi
